const auth = {
  signup: "/auth/register",
  login: "/auth/login",
  logout: "/auth/logout",
};

const workspaces = {
  getWorkspaces: "/workspaces",
  createWorkspace: "/workspaces",
};

export const URLs = {
  auth,
  workspaces,
};
